{
  "fields": [
    "employee_id",
    "absence_type_external_id",
    "status",
    "start_date",
    "end_date",
    "start_half_day",
    "end_half_day",
    "amount",
    "unit",
    "employee_note"
  ],
  "mapped_fields": {
    "employee_id": {
      "target_field": "id",
      "ground_truth_path": "/timeoff/employees/{id}/requests (pathParameter.id)",
      "confidence": 0.95,
      "notes": "Employee ID passed as path parameter",
      "mapping_type": "Direct"
    },
    "absence_type_external_id": {
      "target_field": "policyType",
      "ground_truth_path": "/timeoff/employees/{id}/requests (requestBody.policyType)",
      "confidence": 0.95,
      "notes": "Direct mapping to policyType",
      "mapping_type": "Direct"
    },
    "status": {
      "target_field": "status",
      "ground_truth_path": "/timeoff/employees/{id}/requests/{requestId} (response.status)",
      "confidence": 0.9,
      "notes": "Status available in response; not settable on create",
      "mapping_type": "Conversion"
    },
    "start_date": {
      "target_field": "startDate",
      "ground_truth_path": "/timeoff/employees/{id}/requests (requestBody.startDate)",
      "confidence": 0.95,
      "notes": "YYYY-MM-DD",
      "mapping_type": "Direct"
    },
    "end_date": {
      "target_field": "endDate",
      "ground_truth_path": "/timeoff/employees/{id}/requests (requestBody.endDate)",
      "confidence": 0.95,
      "notes": "YYYY-MM-DD",
      "mapping_type": "Direct"
    },
    "start_half_day": {
      "target_field": "startDatePortion",
      "ground_truth_path": "/timeoff/employees/{id}/requests (requestBody.startDatePortion)",
      "confidence": 0.9,
      "notes": "Map true→morning, false/all-day→all_day; business rules apply",
      "mapping_type": "Conversion"
    },
    "end_half_day": {
      "target_field": "endDatePortion",
      "ground_truth_path": "/timeoff/employees/{id}/requests (requestBody.endDatePortion)",
      "confidence": 0.9,
      "notes": "Map false→all_day; true→afternoon",
      "mapping_type": "Conversion"
    },
    "amount": {
      "target_field": "days|hours",
      "ground_truth_path": "/timeoff/employees/{id}/requests (requestBody.days or requestBody.hours)",
      "confidence": 0.9,
      "notes": "Logical: if unit=DAYS map to days; if unit=HOURS map to hours",
      "mapping_type": "Logical"
    },
    "unit": {
      "target_field": "derived",
      "ground_truth_path": "/timeoff/employees/{id}/requests (derived from days/hours)",
      "confidence": 0.85,
      "notes": "Logical: infer DAYS if days present; HOURS if hours present",
      "mapping_type": "Logical"
    },
    "employee_note": {
      "target_field": "description",
      "ground_truth_path": "/timeoff/employees/{id}/requests (requestBody.description)",
      "confidence": 0.95,
      "notes": "Direct mapping",
      "mapping_type": "Direct"
    }
  },
  "unmappable_fields": [],
  "unmappable_analysis": {},
  "mapping_summary": {
    "direct_mappings": 5,
    "conversion_mappings": 3,
    "logical_mappings": 2,
    "unmappable_fields": 0,
    "mapping_coverage": "100%"
  },
  "api_analysis_notes": {
    "endpoints_analyzed": [
      "/timeoff/employees/{id}/requests (POST)",
      "/timeoff/employees/{id}/requests (GET)",
      "/timeoff/employees/{id}/requests/{requestId} (GET)"
    ],
    "half_day_logic": "Enum: all_day, morning, afternoon",
    "unit_logic": "Derive from days/hours"
  }
}